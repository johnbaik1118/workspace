#include <stdio.h>
#include <string.h>

void main(char* args[]) {
    char* str = "Hello, I am C";
    for(int i =0; i <= strlen(str); i++)
      printf("%c", str[i]);
    printf("\n");
    printf("%s", str);
    printf("\n");
}
